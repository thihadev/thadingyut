{
  "rules": {
    "wishes": {
      ".read": true,
      ".write": true,
      ".indexOn": ["timestamp"],
      "$wishId": {
        ".validate": "newData.hasChildren(['wish', 'timestamp']) && newData.child('wish').isString() && newData.child('timestamp').isNumber()"
      }
    }
  }
}
